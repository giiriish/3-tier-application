- name: Install MariaDB on App Tier
  hosts: app_servers
  become: yes

  tasks:
    - name: Install MariaDB server
      yum:
        name: mariadb-server
        state: present

    - name: Start and enable MariaDB
      service:
        name: mariadb
        state: started
        enabled: yes
