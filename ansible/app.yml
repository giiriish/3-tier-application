- name: Install MariaDB on App Tier
  hosts: app
  become: yes

  tasks:

    - name: Install MariaDB
      dnf:
        name: mariadb-server
        state: present

    - name: Start MariaDB
      service:
        name: mariadb
        state: started
        enabled: yes
